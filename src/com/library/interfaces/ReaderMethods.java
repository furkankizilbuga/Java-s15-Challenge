package com.library.interfaces;

import com.library.books.Book;
import com.library.persons.Person;

public interface ReaderMethods {
    void purchaseBook(Book book);
    void borrowBook(Book book);
}
